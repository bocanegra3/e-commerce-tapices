const numeros = [1,2,3]
const numerosnuevos = numeros.concat([4,5,6])

//spread operator

const numerosSpread = [...numeros,4 ,5 ,6]

//Destruturing de Arrays

const colores =['rojo', 'amarillo', 'violeta']

const primero= colores[0]
const segundo= colores[1]

const[primerColor, segundoColor]= colores

if (edad>= 18){
    mensaje='sos mayor, poder tener cheques'
}else {
    mensaje='no podes tener cheques'
}

const superMensaje= edad>= 18 ? 'podes comprar cervezas' : 'no podes comprar cerveza'

//se simplifica el codigo

